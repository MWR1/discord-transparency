/**
 * Represents an HTML element, or null. This type exists mainly
 * for getting elements from the DOM, through functions such as "getElementById", as their return type is this.
 */
type DOMElement = HTMLElement | null;
export default DOMElement;
